# Database Configuration (Neon Serverless Postgres)
# Replace these with your actual Neon connection strings
# DATABASE_URL: Pooled connection string (with -pooler suffix) - Recommended for queries & transactions
DATABASE_URL="postgresql://user:password@ep-wandering-water-123456-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"

# DIRECT_URL: Direct unpooled connection string - Required for Prisma migrations & DDL
DIRECT_URL="postgresql://user:password@ep-wandering-water-123456.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (for authentication)
JWT_SECRET="{{JWT_SECRET}}"
