# Database Configuration (Neon Serverless Postgres)
# Replace these with your actual Neon connection strings
DATABASE_URL="postgresql://user:password@ep-wandering-water-123456.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (for authentication)
JWT_SECRET="{{JWT_SECRET}}"
